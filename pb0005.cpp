#include <bits/stdc++.h>
using namespace std;

// Đếm số chia hết cho 3 trong đoạn [1, n] (1 ≤ n ≤ 10^18)

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    long long n;
    cin >> n;
    cout << n / 3;
    return 0;
}
